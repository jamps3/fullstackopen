{
  "name": "notebackend",
  "version": "0.0.1",
  "description": "",
  "license": "MIT",
  "author": "Jan-Erik Labbas",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "build": "rm -rf dist && cd ../part3-notes-frontend/ && npm run build && cp -r dist ../part3-notes-backend/",
    "start": "node index.js",
    "dev": "node --watch index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^5.1.0"
  }
}
